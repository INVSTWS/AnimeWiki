<%= form_with(url: animes_path, method: :get, local: true, class:"d-flex") do |form|%>
    <%= text_field_tag(:search_by_title, params[:search_by_title],  class:"form-control me-2", placeholder:"Search")%>
 <%= submit_tag("Search", class:"btn btn-outline-success") %>
<% end %>